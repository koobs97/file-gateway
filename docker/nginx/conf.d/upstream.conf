# Blue-Green upstream 변수 (deploy.sh가 docker cp로 자동 갱신)
# 현재 활성: blue
#
# upstream 블록 대신 변수 방식 사용:
#   - nginx 시작 시 DNS resolve 없이 기동 가능 (컨테이너 미존재 시에도 OK)
#   - 요청 시점에 Docker DNS(127.0.0.11)로 동적 resolve
set $backend_host  "file-gateway-blue";
set $frontend_host "file-gateway-frontend";
