# =====================================================================
# Blue-Green 업스트림 설정 (배포 스크립트가 동적으로 교체)
# deploy.sh가 이 파일을 수정한 후 nginx -s reload로 무중단 전환
# =====================================================================

upstream backend {
    server file-gateway-blue:8080;
}
